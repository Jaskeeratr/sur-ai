# Model Artifacts

Run `python training/train_stability_model.py` from the `server` directory after installing `requirements-ml.txt`.

The script writes `stability_model.pt` here. The model binary is ignored by Git, while this README keeps the folder documented.
